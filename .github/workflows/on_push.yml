name: 'Release Docker'

on:
  push:
    branches:
      - version-14-hotfix
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docker-release:
    name: 'Trigger Docker build on release'
    runs-on: ubuntu-latest
    steps:
      - id: docker
        run: |
          if [[ '${{ github.event_name }}' == 'pull_request' ]]; then
            DOCKER_TAG="pr-${{ github.event.number }}"
          else
            DOCKER_TAG=`echo ${GITHUB_SHA} | cut -c1-8`
          fi
          echo "DOCKER_TAG: ${DOCKER_TAG}"
          echo "gh-docker-tag=${DOCKER_TAG}" >> $GITHUB_OUTPUT
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.PAT }}
          repository: bacsicayxanh-hcm/frappe-docker
          event-type: build-docker
          client-payload: '{"ref": "main", "gh-docker-tag": "${{ steps.docker.outputs.gh-docker-tag }}"}'
